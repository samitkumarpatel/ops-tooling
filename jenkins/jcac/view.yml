jenkins:
  views:
  - all:
      name: "all"
  - list:
      name: "list01"
      columns:
      - "status"
      - "weather"
      - "jobName"
      includeRegex: "pipeline*"
  primaryView:
    all:
      name: "all"